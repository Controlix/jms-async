server:
  port: 8120
  servlet:
    context-path: /jms-async

management:
  endpoint:
    health:
      show-details: 'always'
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: '*'

spring:
  artemis:
    mode: native
    #    broker-url: tcp://localhost:61616?jms.useAsyncSend=true
    user: admin
    password: superman

debug: true